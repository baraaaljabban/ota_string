name: example

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  ota_string: any
  rxdart: ^0.27.2

dev_dependencies:
  build_runner: any
  lints: ^1.0.1
  ota_generator: any

dependency_overrides:
  ota_string:
    path: ../../ota_string/
  ota_generator:
    path: ..